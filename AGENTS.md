# video-scaffold 项目规则

本项目提供本地视频制作工具，不替用户决定选题、旁白或是否投稿。业务入口为 `run.ps1` 和 `pipeline.workflow`；旧定制生成器已退出活动树，历史保留在 Git，不恢复第二套生产入口。

- `doctor`、`status`、`plan`、`verify` 和 `cleanup --dry-run` 是只读检查；真实浏览器/GPU 探针用 `doctor-local`，外部 TTS 探针用 `doctor-live`。静态检查不会读取密钥文件、下载模型或生成视频。
- 每阶段消费当前上游编号、输入指纹和输出字节；不得绕过失败校验、伪造生产配音/时间轴的来源记录，或把模拟测试当成真实配音质量证明。重复 cue 用 `data-cue-index`，时间偏移用 `data-cue-offset`；语音估计精度与渲染帧时钟分开。
- 生成前景只写静态 SVG。动画由 `seekTime(t)` 驱动；布局与动画属性分离。新增动画必须同时更新运行时、测试及适用创作指引。
- 配音、片段、素材、项目配置和成片属于该期原件/成果，不用演示或初始化覆盖。`demo`/`smoke` 只创建隔离项目；清理只处理确知的可再生中间文件，失败与残留必须报告。
- 默认 auto 复用有效词轴，优先 Fish 原生时间戳；缺失时通过登记的 ChineseASR 解释器调用已知文案对齐。视频项目不加载本地 ASR 模型，不因切换时间轴重配音；旧绑定有效的音频/词轴保留兼容。章节只依赖场景编号、时长和定义。
- 密钥仅在明确配音操作时从环境/本地入口取得，不输出其值。`.video-machine.json` 是未跟踪的机器适配，不写进通用业务配置；本机 GPU 资源通过现有租约协调，不另起服务或暂停别人的工作。
- 开发用 `pwsh -File .\run.ps1 test`；实际集成用 `pwsh -File .\run.ps1 smoke`。保持源码、测试、真实硬件探针、完整短片、人工视觉/听觉检查分别可追溯。
- 完成图像/时间轴变更后先构建与布局检查，再审阅有声预览，最后渲染、合成与验收。`READY` 只表示当前产物的结构和来源通过；人工观看及投稿授权是独立事项。

创作方法按需读 `docs/AUTHORING.md`；运行方法读 README；通用行为和授权不在本项目复制。
